// Test file with intentional formatting issues (all fixable)
const testVar = 'unformatted';

function testFunction() {
  console.log('bad formatting');
  return testVar;
}

export default testFunction;
